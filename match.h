#ifndef SEARCH_BY_MASK_MATCH_H
#define SEARCH_BY_MASK_MATCH_H

#include <string>


namespace MyLib {
    bool match(const std::string &subject, const std::string &mask);
}

#endif //SEARCH_BY_MASK_MATCH_H
